2. Build the Docker Image
docker build -t my-json-server .

3. Run the Container
docker run -d -p 3001:3000 --name json_placeholder_1 jkatnti/json_placeholder_orig:skola

Your API will be accessible at http://localhost:3000. 

Optional: Use Docker Compose
Create docker-compose.yml:

version: '3'
services:
  json-server:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./db.json:/data/db.json

Run with:

docker-compose up -d

